---
"@gracesoft-sentinel/agent-switcher": minor
---

Service map: `/services` (or `menu`) lists every registered agent with its description and command, marks the active one, and offers a tap-to-switch quick reply per agent. Switching also matches a tapped quick-reply id. Exports `renderServiceMap` and `switcherSessionIdFor`.
